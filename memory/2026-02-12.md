# 2026-02-12

## Policy / preferences (email)
- Default mode: **draft-first** (do not send automatically) unless Richard explicitly says “send” for that specific email/thread.
- Accounts authorized for drafting/sending when approved: **both** `richard@thankyouforyourservice.co` and `richducat@gmail.com`.
- Hard guardrails still apply (must escalate instead of sending): finances/bills, cancellations/refunds/chargebacks, angry/upset threads, legal-ish.
- **Still never email Karen back** (rule remains).

## Stripe → Zoho (payments → Deals)
- Goal approved: sync Stripe Live payment/subscription info into **Zoho Deals**.
- Canonical match: Stripe **customer.email** → Zoho Deals **Email_Address**.
- Created Deals custom fields (API names):
  - `Stripe_Customer_ID`, `Stripe_Subscription_ID`, `Stripe_Status`
  - `Stripe_MRR`, `Stripe_Lifetime_Paid`, `Stripe_Refunded_Total`, `Stripe_Last_Invoice_Amount`
  - `Stripe_Last_Payment_Date` (datetime)
- Important Zoho UI gotcha: fields were initially added under **CREATE** layout tab; they must also be added under **DETAIL VIEW** to appear on existing Deal record pages.
- Script: `scripts/tyfys/stripe-sync-to-zoho-deals.mjs`
  - Fixed Zoho PUT call parameter mismatch (was using wrong keys).
  - Added `--scan subscriptions` mode for backfilling beyond recent Stripe events.
  - Ran backfill: scanned ~86 subscriptions → ~85 unique customers → matched ~64 deals → updated ~64 deals.
  - Expanded totals logic to include **all payments** by customer using Stripe **charges** (one-time + subscription) plus invoices.
  - Discovered Zoho datetime formatting strictness for `Stripe_Last_Payment_Date` (must omit milliseconds; use `YYYY-MM-DDTHH:MM:SS+00:00`).

## Writing voice guide (Richard)
- Opening: “Hi <First Name>,”; warm + direct; gratitude early; no corporate fluff/jargon.
- Structure: short paragraphs, bold headers when needed, bullets/numbered next steps, clear CTA, close with “Best, Richard”.

## Ops / automation
- RingCentral migration: Sales Team chatId changed; correct current chatId discovered via API list: **156659499014**.
- Patched RingCentral breakpoints:
  - `ringcentral-inbound-sms-auto-reply.mjs` default tenant -> `new`.
  - `leads-evening-nurture.mjs` now persists rotated refresh tokens back to `memory/ringcentral-refresh-tokens.json`.
  - `ringcentral-oauth-refresh-token-per-user.mjs` default tenant -> `new`.
- New automation: inbound SMS forwarder that maps inbound texts → Zoho Lead owner (Adam/Amy/Jared) and forwards internally when the inbound lands on the wrong line.
  - Script: `scripts/tyfys/ringcentral-inbound-sms-forward-to-owner.mjs`
  - Cron: "TYFYS inbound SMS forward-to-owner (every 30m)" (8am–7:59pm ET, Mon–Sat)
  - State: `memory/tyfys-ringcentral-inbound-sms-forwarder.json`
- SMS autopilot failure seen (needs fix): RingCentral 403 "Phone number doesn't belong to extension" when sending via `sms-autopilot.mjs`.
- Root cause found (NEW tenant): `new:jared` refresh token is authenticated as Adam’s extension (Adam Ayotte extId 1545175051 / ext 109) → causes 403 when trying to send as Jared line.
- Verified via admin API: Jared Maxwell is **extension 105** (extId 1545171051) and owns direct number **+16822675268**.
- Fix: re-auth Jared while truly logged into Jared Maxwell (ext 105) in NEW tenant (use Incognito to avoid session reuse), then store token as `new:jared` and verify `/extension/~` returns Jared Maxwell ext 105.

## Email attachment uploads (clutter issue)
- Reported issue: automation that scans customer emails for uploads is incorrectly uploading inline/signature logo images (business image) as attachments, cluttering uploads.
- Likely fix: filter out inline parts / CID images, common logo filenames, and tiny images; optionally allow-list PDFs.

## Lead-buyer outreach
- Draft templates stored at `tmp/lead-buyers/outreach-emails-richard-2026-02-12.md`.
- 2026-02-12: User authorized sending outreach and explicitly requested email enrichment + to "send all".
- CC Karen override: user explicitly requested CC Karen on this outreach (override the "never email Karen back" rule for this specific campaign).
- Signature provided for outreach (includes confidentiality disclaimer) + add team line: "Team: Karen Hallet (Medical Team)".
- Contact enrichment outputs:
  - `tmp/lead-buyers/lead-buyers-us-2026-02-12_contacts_top25.csv`
  - `tmp/lead-buyers/lead-buyers-us-2026-02-12_contacts_top25.md`
- First wave sent from `richard@thankyouforyourservice.co`, CC `karen@thankyouforyourservice.co`: 11 emails to publicly listed inboxes.

## Recruiting studies (OpenDoor)
- New study screening request: Cutaneous Lupus Erythematosus (CLE) study.
  - Inclusion: 18+, histologically confirmed CLE, active cutaneous manifestations, active despite adequate antimalarial (Plaquenil/hydroxychloroquine) trial.
  - Exclusion: current Hep B/C/HIV/TB; cancer history (except cured BCC/SCC >2y); suicide attempt/ideation <1y; substance abuse <6m.
- Geo constraint added: candidates must be within 50 miles of site ZIP **24060** (Blacksburg, VA; lat 37.2563, lon -80.4347).
- Candidate scans generated:
  - Deals: `memory/opendoor/lupus-cle-deals-2026-02-12.csv` / `.json` (matchedDeals=49)
  - Leads: `memory/opendoor/lupus-cle-leads-2026-02-12.csv` / `.json` (matched_leads=148)
- Clean tiered doc generated and uploaded to Drive as DOCX:
  - https://docs.google.com/document/d/1kb6p8miTsN7JaUmEQsUVVXddNuhB9WTV/edit

## Identity
- Agent name set to **Cool Cat** (per Richard).

## OpenClaw session reset (stop daily resets)
- Updated gateway config to disable daily session resets by setting:
  - `session.reset.mode = "idle"`
  - `session.reset.idleMinutes = 525600` (≈ 1 year)

## Backups / persistence (per transcript)
- Slack: joined workspace **Personal ASSist** as user `richducat` via shared invite.
- Created backup scripts:
  - `scripts/backup/git-auto-sync-all.sh` — pull/rebase, auto-commit, push across repos in this workspace.
  - `scripts/backup/drive-backup-openclaw.sh` — tar.gz bundle of `/Users/richardducat/clawd/memory`, `~/.openclaw/openclaw.json`, and `~/.openclaw/memory` sqlite index, then upload to Drive.
- Google Drive target folder (richducat@gmail.com): **Project File Backups** folderId `1lpCE-GMPVoChB3qqTBvLAVB9ekAnmMs3`.
- Added crons:
  - Hourly: **Hourly backup: auto-commit & push all repos (richducat)** (runs at `:05` each hour ET), jobId `d43e5f81-9be4-43ff-8e6d-bc8082ef99ab`.
  - Nightly: **Nightly backup: OpenClaw state bundle → Google Drive (richducat)** (2:30am ET), jobId `188a18be-88ee-4b81-95e0-8e7d1a9a236a`.
- Updated `.gitignore` to avoid committing huge/build artifacts and to keep token/state JSON out of git (Drive bundle is the backup path for that data).

## RingCentral: Jared disabled to prevent batch failures
- Confirmed NEW-tenant token mapping: `new:jared` was resolving to **Adam Ayotte (ext 109)** at the time; others (richard/devin/adam/amy) resolved correctly.
- Per Richard: **skip Jared for now** so he doesn’t block automations.
- Implemented: inbound SMS forward-to-owner now excludes Jared:
  - REP_LINE_BY_OWNER now includes only Adam + Amy.
  - Extension scan excludes Jared.
  - If Zoho owner name includes Jared, mark processed as `owner_jared_disabled` and skip.
- Updated cron job "TYFYS inbound SMS forward-to-owner (every 30m)" to state Jared is excluded.
- Commit: `fix(rc): disable Jared in inbound SMS forwarder` (hash `12b000a`).

## Slack (Personal ASSist) connected
- Added Slack Socket Mode tokens and restricted Slack usage to two channels:
  - `C0AEUS1FYTW`
  - `C0AEJREP291`
- Require @mention in those channels; Slack DMs disabled.
- Confirmed working in-channel.

## Guide replication: workspace.md + markdown drift audit
- Created `workspace.md` as the local reference doc.
- Added docs snapshot stub: `docs/reference/openclaw-docs-home.md`.
- Added daily drift check script: `scripts/maintenance/markdown-audit.mjs`.
- Scheduled cron: "Daily markdown audit (drift check)" (3:15am ET), jobId `60979461-b8c7-4680-8d2b-04cdefd74389`.
- Commit: `chore: add workspace.md + daily markdown audit` (hash `3363242`).

## Telegram “topics as folders” setup request
- Richard wants to mirror the tutorial structure: **Telegram as primary interface** using a group with **Topics** as narrow channels (kids, court, TYFYS sales/ops, automations, LabStudio, notes/transcripts like Plaud/Supernormal, etc.).
- He asked the agent to create the topics; UI automation via Peekaboo was attempted but blocked on macOS permissions (Peekaboo not grantable as an app on this machine per prior attempt).
- Plan/approach: use **Chrome Browser Relay** attached to a Telegram Web tab to create/manage Topics in the group UI.
- User feedback: strongly prefers “do the full setup” (no scope-minimizing); is annoyed by perceived under-delivery/"baiting" and wants direct execution.

## Noise control
- Richard requested to stop repeated hourly notifications about outbound SMS autopilot failures; updated cron job `TYFYS outbound SMS autopilot (Adam/Amy/Jared, NEW tenant)` to `delivery.mode="none"` so it runs silently.

## Telegram topics created (in-group)
- Using Chrome Browser Relay + Telegram Web to create Topics (Peekaboo UI automation not available).
- Topics created so far in the Telegram group:
  - OVERVIEW (read this first)
  - CRON / AUTOMATIONS (status + failures)
  - INBOX TRIAGE (needs Richard)
  - TYFYS — Sales Performance
  - TYFYS — Ops / Provider Replies
  - TYFYS — Zoho Admin / Data Hygiene
- Remaining topics to create include: TYFYS RingCentral SMS, Kids (family admin / Berkeley speech / Everett school), Court/Legal, Plaud, Supernormal, Meeting Prep, LabStudio, OpenClaw/Backups.
- Note: Browser control was intermittently flaky (element not found / timeout); may require tab refresh + re-attach.

## Nightly build (2026-02-12)
- Added `--redact` option to `scripts/tyfys/daily-sales-ops-brief.mjs` to mask client phone numbers/names in the RingCentral sections when sharing the brief internally.
- Added `--selftest` mode to verify redaction output without hitting APIs.
- Branch: `chore/2026-02-12-sales-ops-brief-redact`
- Commit: `ebc3aa6`
- PR draft: `PR_DRAFT_2026-02-12_sales-ops-brief-redact.md`
