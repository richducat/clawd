# 2026-02-14

## Paid Media Buyer Pro (PMBP)
- KickCraft: user wants both a Temple Runner-style mode and an FC Mobile-style match mode; organized repo `richducat/kickcraft` under `web/` and added `web/fc-street/index.html` shell with both mode buttons (match + runner stub), plus README. Commit `8b47c21`.
- Shipped plain-English copy rewrite on homepage to remove jargon (UGC/hooks/angles/DR). Headline now: “Tell us what your business does. We’ll write your ads and a simple launch plan.”
- Wizard now supports all 3 approved templates with full plan outputs:
  1) Local Service → Google Search (calls + form leads)
  2) Online Store → Meta Ads (purchases)
  3) B2B/High-ticket → Google Search (booked calls)
- Success page updated with clearer “What happens next” and CTA to generate ad kit.
- Deployed to prod: https://paid-media-buyer-pro.vercel.app (commit `7fb73ef`).

### PMBP lead capture (demo-mode traffic)
- Google Sheet created in richducat@gmail.com: "PMBP Leads" (sheetId `1ziQDyVUWAjmfUWe359KxM3Mzje568rmKU3OMKzKRDf0`).
- App has `/api/leads` endpoint + wizard requires email on approve and posts lead before redirecting to demo success (commits `b241938` and later wizard updates).
- Still blocked from actually appending to the sheet from Vercel until a Google Apps Script Web App endpoint URL is created and Vercel env vars set:
  - `LEADS_SHEET_ID`
  - `LEADS_APPS_SCRIPT_URL`

## LabStudio
- Production app at https://app.labstudio.fit was showing Next.js starter page on `/` (looked like pages/tabs disappeared). Fixed by changing root route to `redirect('/members')`.
- Deployed fix to prod (commit `c25eb35`).
- User reported missing "Me" tab + Stripe shop items; confirmed via git history that Stripe shop + checkout and Profile/Me existed previously.
- Restored Stripe shop code + UI and re-added "Me" tab:
  - APIs: `/api/lab/shop` and `/api/lab/shop/checkout` plus `src/lib/stripe.ts`.
  - UI: `MarketView` no longer placeholder; "Me" tab added to bottom nav.
  - Added `stripe` dependency to satisfy Vercel build.
  - Deployed to prod (commits: `ec8aace`, `8ea215c`, `560ed4d`).
- Set/updated Vercel Production env var `STRIPE_SECRET_KEY` by reading Desktop `TDEVCODE.rtf` (user explicitly authorized) and redeployed; shop should now load real Stripe products.

## Other OpenClaw machine stability
- User preference update: stop asking for approvals on obvious default decisions; proceed with the fastest reasonable path by default. Still ask for explicit approval for truly sensitive/irreversible actions (external sends, destructive changes), but do not slow down for routine execution.

- Root cause of "crash after a few hours": gateway LaunchAgent was not installed/loaded, then gateway was blocked from listening until `gateway.mode` configured.
- Fix steps that worked:
  - `openclaw gateway install` then start/restart
  - `openclaw config set gateway.mode local`
  - `openclaw gateway restart` and confirm `openclaw gateway probe` reachable.
- Also identified harmful model fallbacks (included 8k-context GPT-4) causing cron to fail with "Model context window too small"; recommended tightening fallbacks to GPT-5/Codex-only models.

## Rabbit r1 integration incident
- `memory_search` tool currently failing due to OpenAI embeddings API `insufficient_quota` (429) on configured OpenAI API key; use git/history/manual review as fallback until embeddings billing is fixed.
- Attempting Rabbit r1 integration likely introduced/changed OpenAI API key usage for memory search embeddings.

## Telegram (Everett)
- User created Telegram topic "Everett Soccer Development Chat" inside group `-1003864787472` with threadId `134` (link format: https://t.me/c/3864787472/134/135).
- Updated OpenClaw config to enable topic `134` for that group so the bot can route posts there.
- Naming preference: do NOT call it Toby; assistant name should be "Cool Cat" only.
- Plan: run a kid-safe mode in that topic (PR-only changes; no secrets/deploys/destructive actions) and act as a coding teacher.

## KickCraft live links (GitHub Pages)
- Enabled GitHub Pages for `richducat/kickcraft` from `main` + `/docs`.
- HTTPS is enforced; HTTP redirects to HTTPS.
- Links:
  - Hub: https://richducat.github.io/kickcraft/
  - FC Street: https://richducat.github.io/kickcraft/web/fc-street/
  - 11v11: https://richducat.github.io/kickcraft/web/kickcraft-11v11/

## clawd backup warning fix
- Fixed git-auto-sync warning about embedded git repo `kickcraft` being tracked in `clawd` (mode 160000). Removed it from `clawd` tracking (`git rm --cached kickcraft`) and added `kickcraft/` to `.gitignore` so backups stop warning.

## KickCraft mobile reliability + PWA
- User wants KickCraft to be mobile-first and prep for App Store; chose path A: web-first PWA.
- Root cause of "KickCraft couldn't start" / "Script error" on mobile was blocked external CDNs (cross-origin script failures).
- Fixes shipped to `richducat/kickcraft`:
  - Vendored JS libs (no external CDN): Three.js r128, React 18, ReactDOM, Babel under `web/vendor/` and `docs/web/vendor/`.
  - KickCraft 11v11: added touch controls (press/drag/release shoot; 2-finger tap pass; swipe switch player).
  - Added PWA app shell in `docs/`: `manifest.webmanifest`, `sw.js`, icons; updated `docs/index.html` to be SPA-style with in-app iframe player.
  - Service worker caching caused some "blank screen" / stuck-old-build behavior; bumped SW cache version and forced update on load.
  - FC Street: user provided a Desktop file "Soccer app working code.txt" with a playable FC Street engine; replaced `kickcraft/web/fc-street/index.html` with that working implementation and kept it mobile-safe (vendored Three.js, removed Tailwind CDN).
- Live hub: https://richducat.github.io/kickcraft/ (HTTPS enforced).
