# 2026-02-19

## TYFYS onboarding domain + Vercel
- Confirmed Vercel project `tyfys-benefits` exists (latestProductionUrl `https://tyfys-benefits.vercel.app`) and linked local repo `/Users/richardducat/repos/tyfys-benefits` via `npx vercel link`.
- Added domain `app.tyfys.net` to Vercel project `tyfys-benefits`.
- Instructed Namecheap DNS A record: `app.tyfys.net -> 76.76.21.21`.
- Verified live:
  - `dig app.tyfys.net A` returned `76.76.21.21`
  - `https://app.tyfys.net` and `https://app.tyfys.net/app.html` return 200.

## Stripe → Zoho webhook fix (production)
- Shipped production fixes in `tyfys-benefits`:
  - In `api/stripe-webhook.js`, changed Deal `Stage` from `'Paid'` to Zoho-valid `'Payment complete'`.
  - Updated `Lead_Source` from `'vaclaimteam.com'` to `'tyfys.net'` in `api/intake.js` and `api/stripe-webhook.js`.
- Deployed to prod and aliased to `https://app.tyfys.net`.

## Backup automation
- Fixed `scripts/backup/git-auto-sync-all.sh` to handle missing upstream branches and autodiscover repos under `~/repos`.
- Verified the script runs cleanly across repos and can set upstream via `git push -u origin HEAD` when needed.

## Deal file health scanner bug
- `scripts/tyfys/deal-file-health.mjs` threw `ReferenceError: stages is not defined` while generating JSON output.
- Fixed by outputting an explicit `stages` list in JSON and pushed to `clawd`.

## Comms proof: Kent Broecker outreach
- Located evidence we emailed Kent Broecker:
  - Sent from `richard@thankyouforyourservice.co` on 2026-02-18 19:17 ET
  - msgId `19c7341fcb3b1fac`, thread `19c61eca8ff60112`, subject “Re: Quick check-in — we’re still on it”.
- Drafted short message for Karen: we did respond and Kent replied back.
