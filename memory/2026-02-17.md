
## 2026-02-17 — durable notes (AM)
- RingCentral scheduled post failures were due to using an invalid/migrated team chatId (`144856375302`) which RingCentral returns as 404 CMN-102. Confirmed the current valid “Sales Team” chatId is `156659499014`.
- Fixed by reverting RC-posting cron jobs back to `chatId=156659499014` and force-running:
  - Morning Sales Team update
  - Lead buckets update
  - Sales KPI scoreboard
  All confirmed posting successfully after revert.
- LabStudio Vercel deploy blocker “No Next.js version detected…” was caused by Vercel Project Root Directory misconfiguration.
  - Patched Vercel project `labstudio-app` rootDirectory from `.` to `labstudio-app` via Vercel API (project id `prj_zJLPfkq24ZjKnWdsdMy5G49XcESi`).
- User preference update: I am allowed to open/post PRs without asking for approval first (still no direct deploys/push-live; PRs are fine by default).
