# 2026-02-10

## Nightly build (11pm ET) — ONE deliverable

### A) What shipped
**TYFYS throughput/revenue unblocker:** updated `scripts/tyfys/fulfillment-tasker.mjs` to be **stage-aware about when “Provider” is required**, preventing noisy “fill required fields” tasks on early-stage deals.

Branch: `chore/2026-02-10-fulfillment-tasker-stage-rules`
Commit: `ffe7889`
PR draft: `PR_DRAFT_2026-02-10_fulfillment-tasker-stage-rules.md`

### B) Why this is the highest-priority move
This tasker is supposed to keep deals moving without you. Requiring Provider too early creates tasks that can’t be completed yet → alert fatigue → people ignore automation. Making Provider conditional keeps the discipline while matching actual workflow timing.

### C) Implementation notes (PR-sized)
- Added `shouldRequireProvider()`:
  - Provider required if Stage contains “provider”, or Appointment Status looks scheduled/confirmed/completed/etc.
  - Otherwise Provider not required yet.
- Updated task Description to reflect this rule.
- Added CLI args: `--sinceDays` (default 14) and `--limit` (default 200; capped at 200).
- Added clearer stdout summary + list of created tasks.

Net change: +74/-13 lines (1 file).

### D) How Richard can test tomorrow
- `node scripts/tyfys/fulfillment-tasker.mjs --dry-run`
- Faster: `node scripts/tyfys/fulfillment-tasker.mjs --dry-run --sinceDays 3`

Expected:
- Early-stage deals missing Provider should no longer get Provider flagged.
- Provider-stage or scheduled-appt deals should still require Provider.

### E) Risks / rollback / next steps
- Low risk; only affects this automation script.
- Rollback: revert commit `ffe7889`.
- Next: replace regex matching with exact Appointment Status picklist values once confirmed (reduces guesswork).
