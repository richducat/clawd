# 2026-02-01

## LabStudio
- Decision: **No mock data** in the user-visible UI going forward. If it shows, it must come from real DB rows or real integrations (seed allowed, but still stored in DB).
- Auth/gating: keep simple **access-code gate** (`LABSTUDIO_ACCESS_CODE`) + session cookie; stable user identity via `labstudio_uid` cookie.
- Fixed bug: Quick Log save was failing when `labstudio_uid` cookie missing; updated middleware to ensure `labstudio_uid` exists and to protect `/api/lab/*`.
- DB: Neon via `DATABASE_URL` (not deprecated `@vercel/postgres`). Added tables for:
  - `lab_users` (+ `onboarding_complete`)
  - `lab_user_profile` (in progress)
  - `lab_daily_stats`
  - `lab_nutrition_log`
- Google Calendar bookings: created Google Calendar **"LabStudio - Bookings"** under `richducat@gmail.com` and added iCal feed URL env var `LABSTUDIO_BOOKINGS_ICAL_URL` in Vercel. Home reads next booking from iCal feed.
- UI: Home tab fully ported (agenda, progress tiles editor, quick actions, wide desktop layout). Added real pages/views for all nav tabs (Book, Nutrition, Habits, Messages, Community, Challenges, Wearables, Squad, Vault, Games, Shop) with non-mock empty states.
- Permission: Richard explicitly authorized continuing work + production deploys overnight to get demo-ready.

## Weekly game plan (working)
1) Integrate onboarding/intake flow into Next.js app as `/onboarding`.
2) Persist onboarding answers → Neon (`lab_user_profile`) and set `lab_users.onboarding_complete=true`.
3) Gate `/members` so users without profile are redirected to onboarding.
4) Remove remaining hardcoded values from UI; Home reads profile + stats + nutrition + bookings from Neon/Google Calendar.
5) Build out Workout session (timers/sets/intervals) with Neon persistence (workouts, sets, PRs) and XP/level updates.
6) Booking: move from read-only iCal to true booking creation (Google Calendar API OAuth) or integrate Calendly if preferred.
7) Community/messages: real feed + posts in Neon; connect to Toby chat for coaching.
8) **Games**: real gamification loops (XP challenges, streaks, leaderboards) backed by Neon; no fake scores.
9) **Shop/Payments**: Stripe products + checkout + entitlements backed by Neon; no placeholder links.
