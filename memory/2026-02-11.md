# 2026-02-11

## TYFYS ops
- Added resilient retry/backoff handling for transient Zoho CRM API failures (429/502/503/504/408 + intermittent network errors) across all scripts using `scripts/lib/zoho.mjs`. Branch: `chore/2026-02-11-zoho-retry-backoff`. Commit: `cd92d61`.
- Reduced Telegram update noise + prevented late-night sends: updated schedules for two cron jobs to run **every 30 minutes only in approved PT windows** (tz=America/Los_Angeles, expr `*/30 9-11,16-19 * * *`):
  - Inbound SMS auto-reply scanner (Sales team) jobId `786870c7-a69b-426c-bd29-3dad3f438003`
  - Outbound SMS autopilot (Adam/Amy/Jared, NEW tenant) jobId `0aa2a6d7-2921-43d7-9242-c7c75c75122d`
- RingCentral NEW tenant issue surfaced: inbound SMS auto-reply scanner failed with **invalid_grant / token revoked** for **user=richard**; requires re-auth via authorize URL + `ringcentral-oauth-refresh-token-per-user.mjs`.
- Provider handoff tasker run: `node scripts/tyfys/provider-handoff-tasker.mjs` → deals_scanned=32, tasks_created=16.
- Hardened provider handoff tasker against duplicate tasks even if local state is lost: added **Zoho-side de-dupe** (checks for existing open Task on same Deal + due date + subject prefix). Commit: `e74fdc7`.
- Added proactive sales-rep SMS SLA outreach: updated `scripts/tyfys/sms-autopilot.mjs` to send follow-up SMS from rep-owned lines for Zoho Leads untouched for **48h** (skipping STOP/DNC + disqualified Lead_Status heuristics). Commit: `935e69a`. Updated cron job `0aa2a6d7-2921-43d7-9242-c7c75c75122d` payload to include `--leadSlaHours 48 --leadLimit 120`.

## Zoho lead conversion mapping
- Found the correct Zoho UI page: Setup → Modules → Leads → **Lead Conversion Mapping** (`/settings/modules/Leads/conversion-mapping`). Observed at least one likely mis-map (Treatment Records mapped to Contact Health Records). No save applied yet.

## LabStudio
- Built DB-backed Habits feature (replaced placeholder):
  - Added tables `lab_habits`, `lab_habit_checkins` in `labstudio-app/src/lib/db.ts`
  - Added API route `/api/lab/habits`
  - Updated UI `labstudio-app/src/app/members/views/HabitsView.tsx` (add habit + toggle today)
  - Verified `tsc --noEmit` and `next build` pass; dev server started on `http://localhost:3000`.
