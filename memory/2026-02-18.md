# 2026-02-18

## TYFYS — Veteran Files → Zoho Deal attachments backfill
- Completed a full backfill pass from “Veteran Files” Drive folders to Zoho Deal attachments.
- Results (this pass): folders processed (with files)=107; matched to Zoho Deals=81; attachment candidates examined=1,330; new attachments uploaded=203; skipped as already-attached by exact filename=1,084; unmatched folders remaining=26; multiple matches=0.
- Errors: 43 upload/read failures, primarily `.gdoc` Google Docs placeholder files causing “Unknown system error -11” hydration/read errors; `.gdoc` must be exported to real PDF/DOCX before attaching to Zoho.
- Report: `memory/tyfys/veteran-files-attach-report.json`.
- Next buckets: (1) resolve 26 unmatched folders (better matching or manual mapping), (2) export + attach `.gdoc` files then rerun errors-only.

## VoltGuard — homepage lead capture
- Replaced the prior multi-step wizard on `voltguard.homes` contact section with the newer Tailwind-based lead form (“Claim Your Free EV Station Diagnostic & Performance Tune-Up”).
- Wired homepage form submission to Google Form `formResponse` endpoint (mode `no-cors`, `application/x-www-form-urlencoded`) using extracted `entry.*` IDs; mapped name/email/phone and placed property + chargers count into the Google Form “issue/details” field; set safe defaults for fields not collected (interest=Maybe, brand=Other, zip='').
- Deployed to GitHub Pages: commit `7459bc3` in `richducat/voltguard`.

## TYFYS — Devin group visibility + stage health
- User requested posting updates to the shared Devin Telegram group so he stays in the know.
- Posted Veteran Files backfill summary into group (`-1003603383366`).
- Scheduled automated Devin-group updates:
  - Morning 9:00 AM ET: “TYFYS — Morning client status (stages 1–3) to Devin group” (cron jobId `0b3dcb20-1397-448c-95f6-d9ea73af8c81`).
  - Evening 6:00 PM ET: “TYFYS — Evening client status (stages 1–3) to Devin group” (cron jobId `db970782-4cb7-48ca-8c70-2b74fbf00a9d`).
- Ran deal stage health scan for first 3 stages (Deals scanned=46) and posted the highest-risk gaps list (attachments missing + overdue task pileups) into Devin group. Raw report saved: `memory/tyfys/deal-file-health-active-stages1-3.txt`.
- Updated `scripts/tyfys/deal-file-health.mjs` to support `--all` and `--stages` args; committed and pushed to `clawd` repo on branch `feat/2026-02-18-labstudio-member-flows` (commit `8a793c3`).

## Intake notes gap
- Concern: many intake calls were done but notes were not added in Zoho; need a review of active clients in first 3 stages to identify who has all documents + who is missing intake-call notes.

## Communication rule (durable)
- Never blindly respond to anyone. Before drafting replies, review the full thread (or as many prior messages as possible) to understand context.
- Respond in Richard’s tone/speaking style while staying professional and matching the conversation’s style.
- Goal: get an update every week and stay informed.
- Never ask the same question twice.

## Berkeley (kid) details (durable)
- Berkeley is **6 years old**, **a girl**, born **2020-01-30**.

## Nightly build (2026-02-18)
- Shipped: `scripts/tyfys/deal-file-health.mjs` now flags likely **missing intake notes** when intake appears completed.
- Branch: `chore/2026-02-18-deal-health-intake-notes`
- PR draft: `PR_DRAFT_2026-02-18_deal-health-intake-notes.md`
- Test:
  - `node scripts/tyfys/deal-file-health.mjs --hours 168 --limit 120` (look for `⚠️MISSING_INTAKE_NOTES`)
  - or `--format json --out-json zoho_exports/deal-file-health.json`
