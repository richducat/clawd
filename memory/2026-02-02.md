# 2026-02-02

## LabStudio — TODAY goal (Richard)
**Goal:** Finish the *full port of the Home dashboard* with **all tiles working** (no placeholders).

### Definition of done
- Home dashboard matches the intended “The Lab Ultimate” Home layout.
- Every tile on Home is functional end-to-end:
  - reads/writes real data (Neon/Postgres) or real integration data (e.g., iCal booking)
  - has correct empty-states but no fake/mock content
  - errors are handled gracefully (no silent failures)
- Login → Onboarding → Members → Home works reliably.

### Current status / notes
- DATABASE_URL provided (Neon) and added to labstudio-app/.env.local (local-only)
- POST /api/db/init now succeeds.
- User reports onboarding + saving info + nutrition log works.

### Next steps (implementation plan)
1) Inventory Home tiles in UI + map each to a backing API + DB table.
2) Implement missing API routes and DB writes/reads.
3) Update HomeView to use only real data; remove any remaining mock defaults.
4) Add smoke-test checklist and run locally.

### Risks
- Some tiles may require new DB schema changes; keep migrations in /api/db/init.
- Some tiles may rely on external integrations (e.g., Google Calendar) and need env vars.

---

## Nightly build (manual trigger) — TYFYS (2026-02-02)

### A) What I built tonight
- Added a `--redact` flag to the TYFYS Daily Sales Ops Brief script so its output is safe to paste into group chats (masks phone numbers + hides deal/event titles).
- Tightened redaction so RingCentral contact *names* don’t leak when phone numbers are missing, and added `--selftest` so you can validate redaction without any API credentials.

### B) PR-ready changes
- Branch: `feat/2026-02-02-sales-brief-redact`
- Commits:
  - `fdd6155` — `feat: add --redact mode to daily sales ops brief`
  - `a4fa118` — `feat: add selftest + stricter redaction to sales ops brief`
- Diff summary:
  - `scripts/tyfys/daily-sales-ops-brief.mjs`
    - New flags: `--redact`, `--selftest`
    - Phone masking: `***-***-1234`
    - Contact-name suppression in redact mode (shows `Unknown` when no digits)
    - Title redaction: `Deal#<suffix>` / `Event#<suffix>`

### C) How Richard tests it tomorrow
1) Run the no-credentials selftest:
   - `node scripts/tyfys/daily-sales-ops-brief.mjs --selftest`
   - Expect: `Selftest OK`

2) Ensure your normal RingCentral + Zoho env vars are set (same as existing script usage).
3) Run (normal):
   - `node scripts/tyfys/daily-sales-ops-brief.mjs --hours 24 --connectedSec 30 --fewMin 2`
4) Run (redacted):
   - `node scripts/tyfys/daily-sales-ops-brief.mjs --hours 24 --connectedSec 30 --fewMin 2 --redact`
5) Confirm the redacted run:
   - Shows `| REDACTED` in the header.
   - Replaces any phone numbers with `***-***-1234`.
   - If RC provides only a contact name (no number), it shows `Unknown` (doesn’t leak names).
   - Replaces deal/event titles with `Deal#...` / `Event#...`.

### D) Next 1–3 actions
- Richard — If you like the output format, we can extend `--redact` to also mask rep names (optional) — Tomorrow.
- Me — Add a `--redact` default for any “share-to-team” mode if/when we add a Slack/WhatsApp sender — Later.

### E) Compliance & security check
- No client PII/PHI added to the repo.
- Change reduces risk of leaking phone numbers / deal names when sharing summaries.

---

## Nightly build (manual trigger) — TYFYS (2026-02-02 #2)

### A) What I built tonight
- Upgraded `scripts/tyfys/deal-file-health.mjs` to flag **at-risk** deals (overdue tasks, missing attachments for provider handoff stages, stale notes/attachments, too many open tasks).
- Added `--redact` and `--onlyAtRisk` so the report can be safely shared and focused on problems.
- Added `--selftest` so you can sanity-check the helpers without any Zoho credentials.

### B) PR-ready changes
- Branch: `chore/2026-02-02-deal-health-risk`
- Commit:
  - `707c199` — `chore(tyfys): add at-risk flags + redaction to deal file health`
- Diff summary:
  - `scripts/tyfys/deal-file-health.mjs`
    - New flags: `--redact`, `--staleDays` (default 7), `--onlyAtRisk`, `--selftest`
    - Adds `flags=...` to each deal line
    - Sorts output to show highest-signal issues first

### C) How Richard tests it tomorrow
1) Run the no-credentials selftest:
   - `node scripts/tyfys/deal-file-health.mjs --selftest`
   - Expect: `Selftest OK`

2) Run (normal):
   - `node scripts/tyfys/deal-file-health.mjs --hours 168 --limit 40`

3) Run (problems only):
   - `node scripts/tyfys/deal-file-health.mjs --hours 168 --limit 80 --onlyAtRisk`

4) Run (problems only, safe to paste in non-private channels):
   - `node scripts/tyfys/deal-file-health.mjs --hours 168 --limit 80 --onlyAtRisk --redact`

### D) Next 1–3 actions
- Richard — Run the `--onlyAtRisk` output and sanity-check whether the thresholds are right (e.g., MANY_OPEN_TASKS ≥6, staleDays=7) — Tomorrow.
- Me — If thresholds feel off, add simple knobs (e.g., `--manyOpenTasks 6`, `--requireAttachmentsStages ...`) while keeping defaults stable — Next nightly.

### E) Compliance & security check
- Reporting-only; no writes to Zoho.
- `--redact` avoids leaking deal/provider names when sharing the output.

---

## Nightly build (manual trigger) — TYFYS / Second Brain (2026-02-02 #3)

### A) What I built tonight
- Added **full-text doc search** to Second Brain’s ⌘K command palette (searches titles + tags + document body content).

### B) PR-ready changes
- Repo: `second-brain/`
- Branch: `feat/2026-02-02-fulltext-search`
- Commit: `58a4b3f` — `feat: full-text doc search in command palette`
- Diff summary:
  - `second-brain/src/app/api/search/route.ts`
    - New API route that builds/caches a MiniSearch index over `second-brain/docs/**`.
  - `second-brain/src/app/_components/CommandPalette.tsx`
    - Debounced fetch to `/api/search?q=...` for doc results (keeps journals/tags local).

### C) How Richard tests it tomorrow
1) From repo root:
   - `cd second-brain`
   - `pnpm install` (if needed)
   - `pnpm dev`
2) Open: http://localhost:3000
3) Press **⌘K** (or Ctrl+K)
4) Type a word that appears *inside* a doc body (not just the title), e.g. try terms from `second-brain/docs/tyfys/README.md`.
5) Confirm results show matching docs and you can hit Enter to navigate.

### D) Next 1–3 actions
- Richard — Confirm search feels good (speed + relevance). If you want, next we can add highlighted snippets/excerpts — Tomorrow.
- Me — Add simple “top hits” + snippets (first matching line) in the palette UI — Next nightly (optional).

### E) Compliance & security check
- Search indexes local markdown docs only; no external network calls beyond the local Next API.
- No client PII/PHI introduced.

---

## Nightly build (manual trigger) — TYFYS (2026-02-02 #4)

### A) What I built tonight
- Added an **optional Ops Risk Flags** section to the TYFYS Daily Sales + Ops Brief (`--opsRisk`) so you can see “at-risk deal files” in the same WhatsApp-friendly summary.
- Extracted the Deal File Health scanner into a shared library so multiple scripts can reuse the same Zoho related-record scan + formatting.
- Added no-credentials **selftests** for both scripts.

### B) PR-ready changes
- Branch: `feat/2026-02-02-ops-risk-brief`
- Commit: `db8a7c2` — `feat(tyfys): add ops risk scan + shared deal file health lib`
- Files:
  - `scripts/tyfys/daily-sales-ops-brief.mjs`
    - New flags: `--opsRisk`, `--opsHours` (default 168), `--opsLimit` (default 40), `--opsMaxConcurrent` (default 5)
    - When enabled, prints a top list of at-risk deals using the shared scanner
  - `scripts/tyfys/deal-file-health.mjs`
    - Refactored to use the shared lib; retains `--redact`, `--onlyAtRisk`, `--staleDays`, `--selftest`
  - `scripts/tyfys/lib/deal-file-health-lib.mjs` (new)
    - Shared scanner: tasks/notes/attachments, risk flagging + sorting

### C) How Richard tests it tomorrow
1) Selftests (no credentials):
   - `node scripts/tyfys/deal-file-health.mjs --selftest`
   - `node scripts/tyfys/daily-sales-ops-brief.mjs --selftest`
   - Expect: `Selftest OK` for both.

2) Deal health scan (Zoho required):
   - `node scripts/tyfys/deal-file-health.mjs --hours 168 --limit 40 --onlyAtRisk`
   - Optional safe-to-share: add `--redact`

3) Daily brief with ops risk section (Zoho + RingCentral required):
   - `node scripts/tyfys/daily-sales-ops-brief.mjs --hours 24 --connectedSec 30 --fewMin 2 --opsRisk`
   - Optional safe-to-share: add `--redact`

### D) Next 1–3 actions
- Richard — Run the `--opsRisk` brief once and tell me if the “at-risk” list is the right length and the right flags (OVERDUE_TASKS / NO_ATTACHMENTS / STALE_*) — Tomorrow.
- Me — If needed, add knobs for thresholds (e.g., `--manyOpenTasks`, `--staleDays`) to the daily brief wrapper without changing defaults — Next nightly.

### E) Compliance & security check
- Read-only reporting; no Zoho writes.
- Optional `--redact` prevents leaking deal/provider names when sharing the output.
