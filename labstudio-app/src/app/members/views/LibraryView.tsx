'use client';

import PlaceholderView from './PlaceholderView';

export default function LibraryView() {
  return <PlaceholderView title="The Vault" subtitle="Real content library (videos/docs) later." />;
}
