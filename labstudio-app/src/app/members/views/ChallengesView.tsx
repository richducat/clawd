'use client';

import PlaceholderView from './PlaceholderView';

export default function ChallengesView() {
  return <PlaceholderView title="Challenges" subtitle="Real challenge modes + leaderboard, stored in Neon." />;
}
