'use client';

import PlaceholderView from './PlaceholderView';

export default function WearablesView() {
  return <PlaceholderView title="Wearables" subtitle="Real integrations later (Apple Health / etc.)." />;
}
