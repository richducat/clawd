'use client';

import PlaceholderView from './PlaceholderView';

export default function MessagesView() {
  return <PlaceholderView title="Messages" subtitle="Will integrate with Toby chat + coach threads." />;
}
