'use client';

import PlaceholderView from './PlaceholderView';

export default function SocialView() {
  return <PlaceholderView title="Squad" subtitle="Real squads + invites, stored in Neon." />;
}
