'use client';

import PlaceholderView from './PlaceholderView';

export default function MarketView() {
  return <PlaceholderView title="Shop" subtitle="Stripe products + orders later (real, no mocks)." />;
}
